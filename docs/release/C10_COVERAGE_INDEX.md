# C10 Existing Failure Coverage Index

This index is a read-only bootstrap audit. It names repository artifacts that
exist at audited handoff commit `b448fcb10d264840e0f918048d4ec975555def7d`.
No test was run and no production behavior was changed while producing it.

## Invariant coverage

| Fault class / invariant | Existing test or report | What it already proves | What remains unproven | New test needed |
|---|---|---|---|---|
| A: provider malformed/failure; fail closed; no phantom source event | `tests/test_binance_provider.py::test_malformed_exact_type_is_rejected`, `::test_http_error_creates_no_synthetic_event`, `::test_disconnect_reports_incident_without_sqlite_write`; `tests/test_polymarket_provider.py::test_malformed_levels_block_whole_batch`, `::test_local_ws_invalid_second_element_enqueues_nothing`, `::test_malformed_json_propagates_without_reconnect`; `tests/test_runtime_core_adversarial.py::test_normal_provider_stream_return_is_fatal`, `::test_missing_binance_minute_blocks_without_live_ready_incident`; `tests/test_runtime_persistence.py::test_recovery_blocked_is_stored_as_critical` | Malformed or failed provider input is rejected, invalid batches enqueue nothing, disconnects surface incidents, and runtime failure/block state is persisted and visible. | One bounded integrated outage-return scenario must jointly assert no phantom signal/fill while unavailable and recovery after the source returns. | **yes** |
| A: provider/network recovery | `tests/test_polymarket_provider.py::test_normal_close_reconnects_and_reports_incident`, `::test_transient_connection_errors_reconnect`, `::test_reconnected_event_reaches_runtime_queue`, `::test_reconnect_does_not_leave_duplicate_heartbeat_tasks`; `tests/test_binance_provider.py::test_reconnect_delay_is_capped_and_jitter_bounded`, `::test_reconnect_delay_resets_after_five_healthy_minutes`; `tests/test_recovery.py::test_ten_minute_binance_gap_is_fully_recovered`, `::test_repeated_recovery_is_idempotent`; `reports/C2_RECOVERY_ACCEPTANCE.json` | Reconnect/backoff behavior, returned Polymarket delivery, deterministic Binance gap recovery, deduplication, and repeated-recovery idempotency are covered. | The same integrated outage-return assertion identified above; do not duplicate malformed payload cases. | **yes — same single A test** |
| C2 restart/recovery; no phantom signal | `tests/test_c2_recovery_hardening.py::test_restart_begins_strictly_after_persisted_cursor`; `tests/test_runtime_core_adversarial.py::test_same_database_restart_replays_identical_market_identity`, `::test_combined_same_db_cutover_restart_gate`; `reports/C2_RECOVERY_ACCEPTANCE.json`; `reports/C2_RECOVERY_TEST_MATRIX.md` | Same-database recovery resumes from persisted identity/cursors, recovery is deduplicated, and recovered evaluations cannot silently become executable. | No separate C10 test; lifecycle-kill coverage is tracked under class D. | **no** |
| C3 rollover/restart; no duplicate lifecycle state | `tests/test_market_rollover.py::test_duplicate_transition_is_idempotent`, `::test_wait_rollover_is_idempotent_after_pass`, `::test_stop_cleans_current_next_and_rollover_tasks`; `tests/test_phase0_rollover_recovery.py::test_next_stream_failure_before_commit_blocks_without_live_pass`; `tests/test_c3_market_rollover_acceptance.py`; `reports/C3_MARKET_ROLLOVER_ACCEPTANCE.json`; `reports/C2_C3_FINAL_ACCEPTANCE.json` | Rollover state is persisted, repeated transitions are idempotent, failed pre-commit cutover remains blocked, and shutdown cleans lifecycle tasks. | One process-termination/restart test at a single lifecycle/checkpoint boundary remains class D; no broad rollover matrix. | **no separate C3 test** |
| C6 exactly-once/replay; no duplicate signal | `tests/test_checkpoint_scheduler.py::test_captured_checkpoint_is_reclaimed_after_restart`, `::test_atomic_completion_creates_one_evaluation_signal_and_outbox`, `::test_exact_completion_replay_is_noop_and_signal_not_duplicated`, `::test_completion_conflict_fails_closed_without_partial_writes`, `::test_concurrent_double_poll_claims_each_schedule_once`; `tests/test_checkpoint_c6_acceptance.py`; `reports/C6_CHECKPOINT_SCHEDULER_ACCEPTANCE.json` | Persistent claims are reclaimed, completion is atomic, replay is a no-op, conflicts leave no partial writes, and concurrent claims remain once-only. | Abrupt process termination at one chosen lifecycle/checkpoint boundary is not proved by these in-process tests. | **yes — one class D test** |
| Paper restart/idempotency; no duplicate fill or double bankroll mutation | `tests/test_paper.py::test_restart_replay_returns_same_rows_without_duplicates`, `::test_restart_replay_rejects_conflicting_immutable_execution`, `::test_mark_and_settlement_persist_signed_pnl_and_bankroll_invariants`, `::test_missing_evidence_blocks_without_intent_or_fill`; `tests/test_mvp_paper_storage.py::test_paper_commit_uses_primary_store_and_existing_resumable_outbox`; `tests/test_mvp_vertical.py::test_current_strict_to_paper_rest_ws_dashboard_vertical` | Exact replay returns the same intent/fill/position, conflicting replay fails closed, settlement replay does not mutate bankroll twice, and missing evidence creates no intent/fill. | Abrupt termination or forced transaction failure during the paper intent/fill/position/account commit is not directly injected. | **yes — one class B/C paper atomicity test, process-level if practical** |
| Paper accounting transactionality; no partial/corrupt accounting | `tests/test_paper.py::test_full_fill_has_deterministic_identity_and_exact_five_share_accounting`, `::test_mark_and_settlement_persist_signed_pnl_and_bankroll_invariants`, `::test_insufficient_cash_and_invalid_states_fail_closed` | Exact five-share accounting, signed PnL/bankroll invariants, idempotent settlement, and fail-closed invalid states are covered on normal transactions. | A forced SQLite busy/transaction failure must prove all paper tables and the account roll back together, with a successful retry after the failure is removed. | **yes** |
| B: SQLite/outbox transaction; no partial signal/outbox | `tests/test_storage_outbox.py::test_outbox_insert_failure_rolls_back_signal`, `::test_signal_insert_failure_creates_no_outbox`, `::test_broker_publishes_only_after_commit_is_visible`, `::test_duplicate_signal_identity_creates_no_duplicate_outbox`; `tests/test_checkpoint_scheduler.py::test_outbox_failure_rolls_back_evaluation_signal_and_schedule`; `tests/test_runtime_core_adversarial.py::test_writer_failure_stops_startup_at_recovery_blocked` | Forced transaction errors roll back signal/evaluation/schedule/outbox changes, publishing follows commit visibility, duplicates remain idempotent, and writer failure blocks startup. | Generic duplication is unnecessary. Only the paper-accounting forced failure named above remains load-bearing for class B. | **no additional non-paper test** |
| C: termination around paper/account commit | Paper restart tests above prove replay after a completed commit, but no existing artifact injects process termination inside the paper/account transaction. | Completed executions are restart-idempotent. | Prove either all paper/account rows commit once or none commit, then prove deterministic recovery/retry with no duplicate fill or bankroll mutation. | **yes — combine with paper B test where feasible** |
| D: termination/restart around one lifecycle/checkpoint boundary | `tests/test_checkpoint_scheduler.py::test_captured_checkpoint_is_reclaimed_after_restart`, `::test_completion_without_signal_is_atomic_and_restart_safe`; `tests/test_process_runtime_integration.py::test_real_process_starting_pass_reconnect_mutex_and_restart`; C2/C3 reports above | Graceful real-process restart works; persisted checkpoint states are recovered in-process without duplicate output. | One abrupt process termination at one selected persisted checkpoint/lifecycle boundary, followed by restart and once-only completion/recovery evidence. | **yes — exactly one boundary** |
| WebSocket reconnect/replay | `tests/test_canary_api.py::test_reconnect_replays_strictly_after_event_id`, `::test_backlog_and_race_window_are_delivered_without_duplicate`, `::test_disconnected_client_does_not_stop_broker_or_replay`; `tests/test_mvp_api_dashboard.py::test_real_paper_state_is_exposed_to_attached_dashboard` | Resume cursor ordering, race-window deduplication, broker survival, and paper events after reconnect are covered. | Nothing in C10 EXPRESS; do not reimplement C8 reconnect coverage. | **no** |
| Single-instance and clean restart | `tests/test_single_instance.py::test_second_instance_is_rejected`, `::test_mutex_is_reusable_after_close`, `::test_shutdown_order_is_exact_and_mutex_is_last`, `::test_stop_is_idempotent`; `tests/test_process_runtime_integration.py::test_real_process_starting_pass_reconnect_mutex_and_restart` | Second process is rejected, mutex is reusable, shutdown order is deterministic/idempotent, and real process restart preserves database integrity. | C9 must verify these properties through its operational launcher; C10 adds no mutex fault test. | **no (C9 acceptance)** |

## Bounded C10 delta

C10 may add only three focused acceptance units unless a reproducible P0/P1
changes the manager-owned scope:

1. One integrated class A source outage/return test.
2. One class B/C paper transaction-or-process interruption test proving atomic
   accounting and retry/replay without duplicates.
3. One class D abrupt restart test at exactly one persisted checkpoint or
   lifecycle boundary.

Backup corruption is C9. WebSocket replay, malformed-provider matrices,
general outbox atomicity, and single-instance behavior are already assigned or
sufficiently covered and must not be duplicated in C10.
