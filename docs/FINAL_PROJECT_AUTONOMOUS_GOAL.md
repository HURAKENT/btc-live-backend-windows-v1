# BTC Daily Range Windows V1 — Operational Goal

## Active terminal objective

`OPERATIONAL_LAUNCH = 100/100`

The accepted Windows backend is launched permanently in
`NORMAL OPERATION / SIGNAL ONLY` mode on `codex/final-project-completion`.

Completion requires integrated operational scheduler semantics, reconnect gap
recovery and accurate liveness, canonical Strict A Dashboard selection, current
authority, bounded Task Scheduler restart policy, one green final native
Windows suite, a validated SQLite backup, verified canonical Scheduler
registration, observable loopback API/Dashboard health, and the backend left
running.

The historical 48-hour C11 protocol is `OPTIONAL / DEFERRED ENDURANCE TEST` and
is not part of this terminal objective. Historical evidence remains immutable.

## Non-negotiable boundaries

`trading_approval=false`, `real_orders=false`, `wallet=false`, `signing=false`,
and `authenticated_CLOB_writes=false`. No PF1 production reconstruction,
real-money path, Windows Service, C12/Linux migration, Data Completion, or full
historical replay is authorized by this goal.

## After launch

Normal lifecycle is `NORMAL OPERATION -> INCIDENT -> NARROW FIX -> CONTINUE
OPERATION`. The next separate, non-blocking research task is
`AUTONOMOUS HISTORICAL REVALIDATION`; do not begin it during operational launch.
